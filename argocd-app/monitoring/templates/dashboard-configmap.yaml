apiVersion: v1
kind: ConfigMap
metadata:
  name: airflow-cluster-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
{{ (.Files.Glob "dashboards/airflow-cluster-dashboard.json").AsConfig | indent 2 }}