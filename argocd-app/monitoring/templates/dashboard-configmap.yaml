apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  {{- $files := .Files }}
  {{- range "dashboards/**.json" }}
  {{ . }}: |-
    {{ $files.Get . }}
  {{- end }}